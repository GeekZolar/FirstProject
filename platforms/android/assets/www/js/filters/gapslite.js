//var gl = angular.module('starter');


// gl.filter('glChequeAccounts', function () {
//    return function (input) {
//      var out = [];
//      for (var i = 0; i < input.length; i++) {
//        if (input[i].AccountType == 'CURRENT ACCOUNT' && input[i].Currency == 'NGN') {
//          out.push(input[i]);
//        }
//      }
//      return out;
//    }
// });

// gl.filter('glNairaAccounts', function () {
//    return function (input) {
//      var out = [];
//      for (var i = 0; i < input.length; i++) {
//        if (input[i].Currency == 'NGN' && input[i].AccountType != 'GT TARGET' && input[i].AccountType != 'GT SPEND2SAVE' && input[i].AccountType != 'POS MERCHANT') {
//          out.push(input[i]);
//        }
//      }
//      return out;
//    }
// });


// gl.filter('nairaAccounts', function () {
//     return function (input) {
//       var out = [];
//       for (var i = 0; i < input.length; i++) {
//         if (input[i].CURRENCY == 'NGN' && input[i].AccountType != 'GT TARGET' && input[i].AccountType != 'GT SPEND2SAVE' && input[i].AccountType != 'POS MERCHANT') {
//           out.push(input[i]);
//         }
//       }
//       return out;
//     }
// })



// gl.filter('glFxAccounts', function () {
//    return function (input) {
//      var out = [];
//      for (var i = 0; i < input.length; i++) {
//        if (input[i].Currency == 'USD' || input[i].Currency == 'GBP' || input[i].Currency == 'EUR') {
//          out.push(input[i]);
//        }
//      }
//      return out;
//    }
// })
